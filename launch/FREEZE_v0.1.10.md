# Launch freeze — v0.1.10 (do not invent npx)

**Primary CTA:** https://caoshurong.github.io/forcepush-ghost/demo/ (Fork-witness A dual-rail, offline)  
**Secondary CTA:** https://github.com/CAOShurong/forcepush-ghost/releases/tag/v0.1.10  
**CLI fixtures:** `node bin/forcepush-ghost.js --fixture fork-witness-a`  
**CLI JSON:** `node bin/forcepush-ghost.js --fixture scandal-a --json`  
**Live fork-witness CLI:** `node bin/forcepush-ghost.js mrdoob/three.js --vs alteredq`  
**Classic scandal:** `node bin/forcepush-ghost.js --fixture scandal-a`  
**npm / npx:** NOT available

## Frozen X / Show HN
See `launch/READY_TO_POST.md`. External post **HOLD**.

## Honesty
- Pages = offline fixtures only; **never** claims live `--vs`
- `--json` is CLI timeline fields only — no secrets/score fake metrics
- Same-repo `--vs` refused (exit 2); API miss → exit 2, no fixture substitution
- No restore / secret-scanner language
