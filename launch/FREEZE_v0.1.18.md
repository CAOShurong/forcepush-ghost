# FREEZE v0.1.18 — 2026-09-18

Tip: Release `v0.1.18` + launch pins on `main` (tip ahead OK — docs-only HOLD/PATHS pins; do not cut `v0.1.19` for docs-only; do not pin ephemeral tip SHAs).

## Ship checklist
- [x] package.json 0.1.18
- [x] CHANGELOG 0.1.18
- [x] Product delta: hard AbortSignal timeout on live / `--vs` (default 60s → EXIT 2; never invent rows / never substitute fixtures)
- [x] PATHS / READY_TO_POST / X / SHOW_HN / NPM_PUBLISH retargeted to v0.1.18
- [x] External X / Show HN: **HOLD** (do not post; only Star PM / user 「发」 lifts)
- [x] npm registry: unpublished — use Pages + `npx github:CAOShurong/forcepush-ghost` / clone Path B (ban registry `npx forcepush-ghost`)
- [x] Pages demo: https://caoshurong.github.io/forcepush-ghost/demo/ (Path B Fork-witness A; offline fixtures only)
- [x] Star PM gate smokes (2026-09-18 ~07:29Z): `npx github:… --help` EXIT 0; live `mrdoob/three.js --vs alteredq` EXIT 0 (WIPED rows); `--timeout 3` EXIT 2

## Tip vs tag
- Pages / `npx github:CAOShurong/forcepush-ghost` → tip (`main`)
- Release URL → `v0.1.18` pin (secondary) @ AbortSignal live timeout
- Docs-only ahead of tag: Viral HOLD packs pin + PATHS secondary pin (+ this FREEZE)

## Story lock
Story/timeline only — not a scanner, not recovery. Never claim we replace GitHub Activity. Do **not** lead marketing with the timeout story.