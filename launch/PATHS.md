# Launch install paths (pre-npm)

1. **Primary — Pages demo:** https://caoshurong.github.io/forcepush-ghost/demo/
2. **Primary — GitHub Release:** https://github.com/CAOShurong/forcepush-ghost/releases/tag/v0.1.2
3. **CLI from clone:** `node bin/forcepush-ghost.js --fixture scandal-a`
4. **npm / npx:** blocked until registry login (`ENEEDAUTH`) — do not claim available

Post-publish: keep Pages as zero-install #1; add `npx forcepush-ghost@0.1.2` as #3.
