# FREEZE v0.1.19 — 2026-09-18

Tip: Release `v0.1.19` + launch pins on `main` (tip ahead OK for docs-only HOLD/PATHS pins after cut; do not pin ephemeral tip SHAs).

## Ship checklist
- [x] package.json 0.1.19
- [x] CHANGELOG 0.1.19
- [x] Product delta: `--vs auto` — capped public forks, pick fork that still holds tip SHA (prefer higher stargazers among holders; never claim best/scandalous); find none → EXIT 2; rate-limit early stop honest
- [x] PATHS / READY_TO_POST / HOLD packs retargeted to v0.1.19
- [x] External X / Show HN: **HOLD** (do not post; only Star PM / user 「发」 lifts)
- [x] npm registry: unpublished — use Pages + `npx github:CAOShurong/forcepush-ghost` / clone Path B (ban registry `npx forcepush-ghost`)
- [x] Pages demo: https://caoshurong.github.io/forcepush-ghost/demo/ (Path B Fork-witness A; offline fixtures only — never live `--vs` / `--vs auto`)
- [x] Star PM gate smokes (2026-09-18 ~08:00Z): `npx github:… --help` EXIT 0 (shows `--vs auto`); live `mrdoob/three.js --vs auto` EXIT 0 (WIPED rows); prior-cycle `--timeout 3` AbortSignal EXIT 2

## Tip vs tag
- Pages / `npx github:CAOShurong/forcepush-ghost` → tip (`main`)
- Release URL → `v0.1.19` pin (secondary) @ `--vs auto`

## Story lock
Story/timeline only — not a scanner, not recovery. Never claim we replace GitHub Activity. Do **not** lead marketing with timeout or auto-fork ranking.